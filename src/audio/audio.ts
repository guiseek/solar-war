import passBy from "./pass-by.ogg";
import alarm from "./alarm.ogg";
import ship from "./ship.ogg";

export const audio = {
  passBy: new Audio(passBy),
  alarm: new Audio(alarm),
  ship: new Audio(ship),
};
